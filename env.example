# === Core Keys ===
# 通义千问（DashScope）API Key —— 必填
DASHSCOPE_API_KEY=

# 可选：网页搜索 Tavily
TAVILY_API_KEY=

# 可选：心知天气
SENIVERSE_API_KEY=


# === Models & RAG ===
# LLM 与 Embedding 默认值（不填则走代码默认）
FORTUNE_LLM_MODEL=qwen-turbo
EVAL_EMBED_MODEL=BAAI/bge-small-zh-v1.5

# CSV 数据路径（Pandas 工具，可二选一）
CSV_FILE_PATH=
CSV_DIR_PATH=


# === Notion Integration ===
# 启用 Notion：1/true/yes 任一
ENABLE_NOTION=0
NOTION_API_KEY=
# 逗号分隔的页面 ID 列表（可无短横线）
NOTION_PAGE_IDS=
# 数据库 ID（可无短横线）
NOTION_DATABASE_ID=


# === Server ===
# 本地开发端口（uvicorn 用）
PORT=8000

# === Database ===
# 数据库配置
# 统一使用 PostgreSQL 向量数据库 (pgvector)
# 开发环境（Docker）: postgresql://my_agent:tangtao0123456@localhost:5432/agent_db
# 生产环境: 设置为实际的 PostgreSQL 连接字符串
DATABASE_URL=postgresql://my_agent:tangtao0123456@localhost:5432/agent_db

# 环境标识
# development: 开发环境，现在也使用 PostgreSQL
# production: 生产部署，使用 PostgreSQL
ENVIRONMENT=development

# === Redis (optional) ===
# e.g. redis://localhost:6379/0  留空表示禁用
REDIS_URL=
# /query 结果缓存秒数（0 禁用）
REDIS_TTL=120


