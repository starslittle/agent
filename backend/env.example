# Deprecated compatibility notice.
#
# Environment variables now have a single source:
#   repository root .env
#
# Copy ../.env.example to ../.env and edit that file. Do not create a second
# backend/.env, otherwise local Python and Docker Compose can drift apart.
